using UnityEngine;

public class KHO_BAU : MonoBehaviour
{
    private void OnTriggerEnter(Collider other)
    {
        //if (other.CompareTag("player"))
        //{
        //    canvas_controller.cur_score = canvas_controller.max_score;
        //}
    }
}
